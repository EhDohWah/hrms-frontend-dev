# =============================================================================
# HRMS Frontend Environment Configuration
# =============================================================================
# Copy this file to .env.development (for development) or .env.production (for production)
#
# SECURITY NOTES:
# - Never commit actual .env files to version control
# - Use HTTPS URLs in production (VITE_REVERB_SCHEME=https)
# - Replace placeholder values with actual secure credentials
# - Keep API keys and secrets secure
# =============================================================================

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Environment (development | production)
VITE_ENV=development

# API Base URL - REQUIRED
# Development: http://127.0.0.1:8000/api/v1
# Production: https://your-api-domain.com/api/v1 (use HTTPS!)
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Public URL for generating links
VITE_PUBLIC_URL=http://localhost:8000

# =============================================================================
# REAL-TIME / WEBSOCKET SETTINGS (Laravel Reverb)
# =============================================================================
# These settings configure the WebSocket connection for real-time features
# like notifications and permission updates.

# Reverb App Key - Get this from your Laravel backend .env (REVERB_APP_KEY)
# IMPORTANT: Replace with your actual key in production
VITE_REVERB_APP_KEY=your_reverb_app_key_here

# Reverb Host
# Development: 127.0.0.1 or localhost
# Production: your-websocket-domain.com (or same as API domain)
VITE_REVERB_HOST=localhost

# Reverb Port
# Development: 8081 (default Laravel Reverb port)
# Production: 443 (for WSS over HTTPS)
VITE_REVERB_PORT=8081

# Reverb Scheme (http | https)
# IMPORTANT: Use 'https' in production for secure WebSocket connections (wss://)
VITE_REVERB_SCHEME=http

# Broadcasting Auth Endpoint
# Development: http://127.0.0.1:8000/broadcasting/auth
# Production: https://your-api-domain.com/broadcasting/auth (use HTTPS!)
VITE_BROADCASTING_AUTH_ENDPOINT=http://localhost:8000/broadcasting/auth

# =============================================================================
# PRODUCTION CHECKLIST
# =============================================================================
# Before deploying to production, ensure:
# [ ] VITE_ENV=production
# [ ] VITE_API_BASE_URL uses HTTPS
# [ ] VITE_REVERB_SCHEME=https
# [ ] VITE_REVERB_PORT=443 (or your WSS port)
# [ ] VITE_BROADCASTING_AUTH_ENDPOINT uses HTTPS
# [ ] VITE_REVERB_APP_KEY is set to actual key (not placeholder)
# [ ] All placeholder values have been replaced
# =============================================================================
